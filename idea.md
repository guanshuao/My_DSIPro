
%% 图四：估计偏差与变异系数(CV)的关系 —— 核心图，直接揭示不均一性影响
% 设计思路：
%   - 横轴：CV_intensity（或CV_amp），作为地表不均一性的量化指标
%   - 纵轴：估计偏差 = BoxCoh_XX - TrueCoh（正值表示高估，负值表示低估）
%   - 绘制多条曲线，分别对应不同窗口大小(3,5,7,9,11,...)
%   - 可添加AdpCoh和ModifiedCoh的偏差作为对比
% 科学意义：
%   1. 揭示Boxcar方法在不均一地表上的系统性偏差方向和大小
%   2. 展示窗口越大，在高CV区域偏差是否越严重
%   3. 对比自适应方法是否有效减轻不均一性带来的偏差
% 预期结论：CV越大（地表越不均一），Boxcar估计偏差越大；大窗口在高CV区域可能高估相干性

%% 图五：最佳窗口大小(Best_Win)与地表不均一性的关系
% 设计思路：
%   - 横轴：CV_intensity 或 SHPNum
%   - 纵轴：Best_Win的频率分布（堆叠柱状图或热力图）
%   - 将CV按区间分组，统计每个区间内各Best_Win值的占比
% 科学意义：
%   1. 揭示不均一地表需要什么样的窗口大小才能获得最优估计
%   2. 验证"均一地表可用大窗口，不均一地表需小窗口"的假设
% 预期结论：高CV（不均一）区域的最佳窗口偏小，低CV（均一）区域最佳窗口偏大


%% 图七：不同窗口大小的RMSE随CV的变化（窗口选择指导图）
% 设计思路：
%   - 横轴：CV_intensity 区间
%   - 纵轴：RMSE = sqrt(mean((BoxCoh_XX - TrueCoh).^2))
%   - 多条曲线对应BoxCoh_3, 5, 7, 9, 11, 13, 15, 17, 19
%   - 可添加AdpCoh的RMSE作为基准线
% 科学意义：
%   1. 为不同均一性条件下的窗口选择提供定量依据
%   2. 找到各窗口的"适用范围"——某窗口在哪个CV范围内表现最好
%   3. 验证自适应方法是否在全CV范围内都优于固定窗口
% 预期结论：小窗口在高CV区表现更好，大窗口在低CV区表现更好

%% 图八：按真实相干性(TrueCoh)分层的偏差分析
% 设计思路：
%   - 将TrueCoh分为低(0-0.3)、中(0.3-0.6)、高(0.6-1.0)三层
%   - 每层内绘制：横轴CV，纵轴估计偏差
%   - 可用子图(subplot)或分面(facet)展示
% 科学意义：
%   1. 揭示不均一性影响是否与真实相干水平有交互作用
%   2. 低相干区vs高相干区，哪个对不均一性更敏感？
%   3. 区分偏差来源：是相干性本身还是地表不均一性主导
% 预期结论：低相干区域对不均一性更敏感，高相干区域估计相对稳健

%% 图九：估计误差的空间分布图（地理可视化）
% 设计思路：
%   - 绘制2x3或3x2的子图矩阵，分别展示：
%     (a) CV_intensity空间分布
%     (b) SHPNum空间分布  
%     (c) |BoxCoh_11 - TrueCoh|空间分布
%     (d) Best_Win空间分布
%     (e) TrueCoh空间分布
%     (f) 修正后误差|ModifiedCoh - TrueCoh|空间分布
% 科学意义：
%   1. 展示不均一性、估计误差的空间结构和相关性
%   2. 直观验证高误差区域是否与高CV区域在空间上重合
%   3. 识别特定地物类型（如边缘、过渡带）的影响
% 预期结论：误差空间分布与CV分布高度相关，边缘/过渡带误差大

%% 图十：不同方法估计偏差的概率密度分布（PDF对比）
% 设计思路：
%   - 横轴：估计偏差 (Estimated - TrueCoh)
%   - 纵轴：概率密度
%   - 绘制多条曲线：BoxCoh_11, AdpCoh, ModifiedCoh, Best_Est
%   - 可按CV分组（高CV vs 低CV）绘制两组子图对比
% 科学意义：
%   1. 展示各方法偏差的分布特征（是否对称、是否有系统偏差）
%   2. 比较各方法的偏差方差（分布宽度）
%   3. 在高CV条件下，哪种方法的偏差分布更集中于零
% 预期结论：高CV时BoxCoh偏差分布右偏（高估），自适应方法分布更对称

%% 图十一：窗口大小增量效应图（边际收益分析）
% 设计思路：
%   - 横轴：窗口增大步长（3→5, 5→7, 7→9, ...）
%   - 纵轴：RMSE减少量（或增加量）
%   - 按CV分组绘制多条曲线
% 科学意义：
%   1. 揭示窗口增大的边际效益在不同均一性条件下的差异
%   2. 在均一地表，窗口增大持续带来改善；在不均一地表，可能存在拐点
%   3. 为"最优窗口"选择提供理论依据
% 预期结论：高CV区域存在窗口大小的"收益拐点"，超过后误差反而增大

%% 图十二：SHPNum阈值敏感性分析（自适应方法验证）
% 设计思路：
%   - 横轴：SHPNum阈值（如10, 20, 30, ..., 100）
%   - 纵轴：使用该阈值筛选后的自适应估计RMSE
%   - 对比：只使用SHPNum>阈值的像素进行估计时的性能
% 科学意义：
%   1. 验证SHP选择策略的有效性
%   2. 找到SHPNum的最优筛选阈值
%   3. 权衡样本量与均一性的trade-off
% 预期结论：存在最优SHPNum阈值，过低引入不均一性，过高样本不足

%% 图十三：相干性高估/低估的条件分析（决策边界图）
% 设计思路：
%   - 横轴：CV_intensity
%   - 纵轴：TrueCoh
%   - 颜色/符号：高估(BoxCoh > TrueCoh)为红色，低估为蓝色
%   - 可拟合决策边界线
% 科学意义：
%   1. 识别Boxcar方法高估/低估的条件组合
%   2. 为后续校正模型提供依据
%   3. 理解物理机制：为什么某些条件下会高估/低估
% 预期结论：高CV+低TrueCoh区域倾向高估，均一地表偏差较小

%% 图十四：时序相干性衰减曲线（如有多时相数据）
% 设计思路：
%   - 横轴：时间基线（temporal baseline）
%   - 纵轴：平均相干性
%   - 按CV分组绘制多条衰减曲线，对比TrueCoh与BoxCoh的衰减速率
% 科学意义：
%   1. 不均一性是否影响相干性衰减特征的估计
%   2. Boxcar方法是否会扭曲时序相干性模型
%   3. 对相位解缠和形变估计的潜在影响
% 预期结论：高CV区域的BoxCoh衰减曲线与真实衰减存在系统偏移

%% 图十五：Bootstrap置信区间比较图
% 设计思路：
%   - 横轴：CV区间
%   - 纵轴：估计相干性
%   - 用误差棒或置信区间带展示不同方法的不确定性
%   - 真值TrueCoh作为参考线
% 科学意义：
%   1. 不仅比较偏差，还比较估计的稳定性/方差
%   2. 在高CV区域，各方法的不确定性如何变化
%   3. 综合评估偏差和方差（bias-variance trade-off）
% 预期结论：大窗口方差小但偏差大，小窗口方差大但偏差小